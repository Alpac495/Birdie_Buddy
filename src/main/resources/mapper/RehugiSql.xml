<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="data.mapper.RehugiMapper">

    <insert id="addComment" parameterType="RehugiDto">
        INSERT INTO rehugi (hnum, unum, rhcontent, rhwriteday)
        VALUES (#{hnum}, #{unum}, #{rhcontent}, #{rhwriteday})
    </insert>

    <insert id="addReply" parameterType="RehugiDto">
        INSERT INTO rehugi (hnum, unum, rhcontent, rhwriteday, ref, step, depth)
        VALUES (#{hnum}, #{unum}, #{rhcontent}, #{rhwriteday}, #{ref}, #{step}, #{depth})
    </insert>

    <select id="getAllCommentsWithReplies" resultType="RehugiDto">
        SELECT *
        FROM rehugi
        ORDER BY ref ASC, step ASC
    </select>

    <delete id="deleteComment" parameterType="int">
        DELETE FROM rehugi WHERE rhnum = #{unum}
    </delete>

    <delete id="deleteReply" parameterType="int">
        DELETE FROM rehugi WHERE rhnum = #{unum}
    </delete>

</mapper>
