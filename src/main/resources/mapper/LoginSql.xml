<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="data.mapper.LoginMapper">
    <insert id="signUser" parameterType="UserDto">
        insert into user (uemail, upass, uname, unickname, uage, ugender) values (#{uemail}, #{upass},
        #{uname}, #{unickname}, #{uage}, #{ugender})
    </insert>
    <select id="getUserData" parameterType="int" resultType="UserDto">
        select * from user where unum=#{unum}
    </select>
</mapper>